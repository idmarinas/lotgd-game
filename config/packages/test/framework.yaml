framework:
    test: true
    session:
        storage_id: session.storage.mock_file
    assets:
        json_manifest_path: ~
        packages:
            lotgd:
                json_manifest_path: ~
