{% translate_default_domain textDomain %}

{{ 'default.paragraph'|t({'name': user.name})|colorize }}

{% if user.goldinbank >= 0 %}
    {{ 'default.balance.positive'|t({ 'balance': user.goldinbank })|colorize }}
{% else %}
    {{ 'default.balance.negative'|t({ 'balance': user.goldinbank|abs })|colorize }}
{% endif %}
