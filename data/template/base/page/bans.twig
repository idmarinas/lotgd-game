{% translate_default_domain textDomain %}

<form action="{{ 'bans.php?op=search'|lotgd_url }}" autocomplete="off" method="POST" class="ui form">
    <div class="inline field">
        <label for="q">{{ 'default.form.search.label'|t|colorize }}</label>
        <div class="ui action input">
            <input type="text" name="q" id="q">
            <button type="submit" class="ui button">{{ 'default.form.search.button.submit'|t|uncolorize }}</button>
        </div>
    </div>
</form>

<br>

{{ include_theme('page/bans/' ~ opt|default('default') ~ '.twig') }}
