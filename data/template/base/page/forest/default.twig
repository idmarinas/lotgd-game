{% translate_default_domain textDomain %}

{% if showForestMessage %}
    {{ 'section.default.description'|t|colorize }}

    {% set result = modulehook('forest-desc') %}
    {% for message in result %}
        {{ message[0]|tl(message[1], message[2])|colorize }}
    {% endfor %}
{% endif %}
