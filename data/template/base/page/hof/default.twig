{% translate_default_domain textDomain %}

<table class="ui very compact striped selectable table">
    <thead>
        <th>{{ 'section.default.table.thead.rank'|t|uncolorize }}</th>
        <th>{{ 'section.default.table.thead.name'|t|uncolorize }}</th>
        <th>{{ 'section.default.table.thead.kills'|t|uncolorize }}</th>
        <th>{{ 'section.default.table.thead.level'|t|uncolorize }}</th>
        <th>{{ 'section.default.table.thead.days'|t|uncolorize }}</th>
        <th>{{ 'section.default.table.thead.best'|t|uncolorize }}</th>
    </thead>
    <tbody>
        {% set rank = attribute(paginator, 'getCurrentPageNumber') * attribute(paginator, 'getItemCountPerPage') - attribute(paginator, 'getItemCountPerPage') %}
        {% for character in paginator %}
            <tr>
                <td>{{ loop.index + rank }}</td>
                <td>{{ character.name|colorize }}</td>
                <td>{{ character.dragonkills|numeral }}</td>
                <td>{{ character.level|numeral }}</td>
                <td>{{ character.dragonage|numeral }}</td>
                <td>{{ character.bestdragonage|numeral }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">{{ 'section.default.table.tbody.none'|t|colorize }}</td>
            </tr>
        {% endfor %}
    </tbody>
</table>

{{ footerTitle[0]|t(footerTitle[1])|colorize }}

{{ navigation_pagination(paginator) }}
