{% translate_default_domain textDomain %}

<span class="ui inverted purple text">
    {% for tpl, params in includeTemplatesPre %}
        {% include tpl with params %}
    {% endfor %}

    {% if showInnDescription %}
        <div class="center aligned"><strong>{{ innName|colorize }}</strong></div>
    {% endif %}

    {{ include_theme('page/inn/' ~ tpl|default('default') ~ '.twig') }}

    {% for tpl, params in includeTemplatesPost %}
        {% include tpl with params %}
    {% endfor %}
</span>

