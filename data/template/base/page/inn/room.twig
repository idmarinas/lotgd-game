{% translate_default_domain textDomain %}

{% if boughtRoomToday %}
    {{ 'section.room.bought'|t|colorize }}
{% else %}
    {{ 'section.room.buy.paragraph0'|t|colorize }}

    {% if user.goldinbank >= bankExpense and bankExpense != expense %}
        {{ 'section.room.buy.fee.paragraph'|t|colorize }}
        {{ ('section.room.buy.fee.' ~ (feePercent ? 'percent' : 'amount'))|t({ 'fee': fee, 'percent': feePercent })|colorize }}
    {% endif %}

    {{ 'section.room.buy.paragraph1'|t|colorize }}
{% endif %}
