{#
    Data avaible in this template

    [
        'texts' => [
            'title' => string
            'desc' => string|array
            'tradein' => string|array
            'nosucharmor' => string|array
            'tryagain' => string
            'notenoughgold' => string|array
            'payarmor' => string|array
        ]
        (Schemas is used for translations purpose)
        'schemas' => [
            'title' => string
            'desc' => string
            'tradein' => string
            'nosucharmor' => string
            'tryagain' => string
            'notenoughgold' => string
            'payarmor' => string
        ]
        'armors' => false|array (Contains all the information of the available armors)
    ]
 #}

{{ ('`c`b`%' ~ texts.title ~ '`0`b`c')|t(schemas.title)|colorize }}

<br>

{# Description of shop #}
{% for desc in texts.desc %}
    {{ desc|t(schemas.desc)|colorize }}
{% else %}
    {{ texts.desc|t(schemas.desc)|colorize }}
{% endfor %}

{# Option to sell your actual armor #}
{% for tradein in texts.tradein %}
    {{ tradein|t(schemas.tradein)|colorize }}
{% else %}
    {{ texts.tradein|t(schemas.tradein)|colorize }}
{% endfor %}

<br>

{# This template generates the table with all the armors #}
{% include 'jade/templates/pages/armor/list.twig' %}
