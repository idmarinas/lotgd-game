{{ '`7You ask %s`7 for a clan membership application form.'|t|format(registrar)|colorize }}
{{ 'She opens a drawer in her desk and pulls out a form.  It contains only two lines: Name and Clan Name.'|t|colorize }}
{{ 'You furrow your brow, not sure if you really like having to deal with all this red tape, and get set to concentrate really hard in order to complete the form.'|t|colorize }}
{{ "Noticing your attempt to write on the form with your %s, %s`7 claims the form back from you, writes %s`7 on the first line, and asks you the name of the clan that you'd like to join:`n`n"|t|format(user.weapon, registrar, user.name)|colorize }}

<table class="ui very compact striped selectable table">
    <tbody>
        {% for clan in clans %}
        {% set clan = modulehook('clan-applymember', clan)  %}
            <tr>
                <td>
                    <a href="{{ ('clan.php?op=apply&to=' ~ clan.clanid)|lotgd_url }}"><i class="check circle icon"></i></a>
                    &#60;{{ clan.clanshort }}&#62;
                    <a href="{{ ('clan.php?op=apply&to=' ~ clan.clanid) }}">{{ clan.clanname|uncolorize }}</a>
                    ({{ clan.count|pluralize('%s member'|t, '%s members'|t) }})
                </td>
            </tr>
        {% endfor %}
    </tbody>
</table>
