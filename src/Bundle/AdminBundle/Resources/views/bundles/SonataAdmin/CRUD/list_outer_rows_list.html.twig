{% for object in admin.datagrid.results %}
    <tr {{ object.isDeleted|default(false) ? 'class="danger"' : ''}}>
        {% include get_admin_template('inner_list_row', admin.code) %}
    </tr>
{% endfor %}
