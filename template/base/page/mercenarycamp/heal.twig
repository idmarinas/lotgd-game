{% translate_default_domain textDomain %}

{% if companionHealed %}
    {{ 'section.heal.paid'|t({ 'companionName': companionName })|colorize }}
{% else %}
    {{ 'section.heal.not.enough'|t|colorize }}
{% endif %}

<br>

{% if companionWounds %}
    {{ 'section.default.mercenary.wounds'|t|colorize }}
{% endif %}
