{% translate_default_domain textDomain %}

{% if mount %}
    {{ ('section.examine.fineBeast.0' ~ random(0, 3))|t|colorize }}

    {{ 'section.examine.creature'|t({
        'name': mount.mountname,
        'description': mount.mountdesc,
        'costGold': mount.mountcostgold,
        'costGems': mount.mountcostgems
    })|colorize }}
{% else %}
    {{ 'section.not.found.beast'|t(_context)|colorize }}
{% endif %}
