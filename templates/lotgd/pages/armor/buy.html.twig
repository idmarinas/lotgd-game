{% translate_default_domain textDomain %}

{% if result %}
    {% if buyIt %}
        {{ 'payarmor'|t({'playerarmor': userPre.armor, 'armorname': result.armorname})|colorize }}
    {% else %}
        {{ 'notenoughgold'|t({'armorname': result.armorname})|colorize }}
    {% endif %}
{% else %}
    {{ 'nosuchweapon'|t|colorize }}
    {{ 'tryagain'|t|colorize }}
{% endif %}
