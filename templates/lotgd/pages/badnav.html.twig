{% trans_default_domain 'page_badnav' %}

{% if user.alive %}
    {{ 'navsCorrupt.village'|trans({'location': user.location})|colorize }}
{% else %}
    {{ 'navsCorrupt.shades'|trans|colorize }}
{% endif %}
