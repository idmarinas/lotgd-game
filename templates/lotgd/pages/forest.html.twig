{% use '{theme}/pages/_blocks/_forest.html.twig' %}

{% translate_default_domain textDomain %}

<!-- Avoid displaying forest text when in battle -->
{% if not battle %}
    {{ block('forest_' ~ tpl|default('default')) }}
{% endif %}
