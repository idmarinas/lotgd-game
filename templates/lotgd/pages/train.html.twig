{% use '{theme}/pages/_blocks/_train.html.twig' %}

{% translate_default_domain textDomain %}

{% if not battle %}
    {{ 'subtitle'|t|colorize }}

    {{ block('train_' ~ tpl|default('default')) }}
{% endif %}
