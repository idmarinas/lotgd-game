{% translate_default_domain 'page_installer' %}

{{ 'stage4.subtitle'|trans|colorize }}
{{ 'stage4.paragraph'|trans|colorize }}

{% if connected %}
    {{ 'stage4.connected.success'|trans|colorize }}

    {# Test create table #}
    {{ 'stage4.connected.test.create.title'|trans|colorize }}
    {% if test.create is same as(true) %}
        {{ 'stage4.connected.test.success'|trans|colorize }}
    {% else %}
        {{ 'stage4.connected.test.fail'|trans|colorize }}
        <blockquote>{{ test.create }}</blockquote>
        {{ 'stage4.connected.test.create.error'|trans|colorize }}
    {% endif %}

    {# Test alter table #}
    {{ 'stage4.connected.test.alter.title'|trans|colorize }}
    {% if test.alter is same as(true) %}
        {{ 'stage4.connected.test.success'|trans|colorize }}
    {% else %}
        {{ 'stage4.connected.test.fail'|trans|colorize }}
        <blockquote>{{ test.alter }}</blockquote>
        {{ 'stage4.connected.test.alter.error'|trans|colorize }}
    {% endif %}

    {# Test index table #}
    {{ 'stage4.connected.test.index.title'|trans|colorize }}
    {% if test.index is same as(true) %}
        {{ 'stage4.connected.test.success'|trans|colorize }}
    {% else %}
        {{ 'stage4.connected.test.fail'|trans|colorize }}
        <blockquote>{{ test.index }}</blockquote>
        {{ 'stage4.connected.test.index.error'|trans|colorize }}
    {% endif %}

    {# Test insert table #}
    {{ 'stage4.connected.test.insert.title'|trans|colorize }}
    {% if test.insert is same as(true) %}
        {{ 'stage4.connected.test.success'|trans|colorize }}
    {% else %}
        {{ 'stage4.connected.test.fail'|trans|colorize }}
        <blockquote>{{ test.insert }}</blockquote>
        {{ 'stage4.connected.test.insert.error'|trans|colorize }}
    {% endif %}

    {# Test select table #}
    {{ 'stage4.connected.test.select.title'|trans|colorize }}
    {% if test.select is same as(true) %}
        {{ 'stage4.connected.test.success'|trans|colorize }}
    {% else %}
        {{ 'stage4.connected.test.fail'|trans|colorize }}
        <blockquote>{{ test.select }}</blockquote>
        {{ 'stage4.connected.test.select.error'|trans|colorize }}
    {% endif %}

    {# Test update table #}
    {{ 'stage4.connected.test.update.title'|trans|colorize }}
    {% if test.update is same as(true) %}
        {{ 'stage4.connected.test.success'|trans|colorize }}
    {% else %}
        {{ 'stage4.connected.test.fail'|trans|colorize }}
        <blockquote>{{ test.update }}</blockquote>
        {{ 'stage4.connected.test.update.error'|trans|colorize }}
    {% endif %}

    {# Test delete table #}
    {{ 'stage4.connected.test.delete.title'|trans|colorize }}
    {% if test.delete is same as(true) %}
        {{ 'stage4.connected.test.success'|trans|colorize }}
    {% else %}
        {{ 'stage4.connected.test.fail'|trans|colorize }}
        <blockquote>{{ test.delete }}</blockquote>
        {{ 'stage4.connected.test.delete.error'|trans|colorize }}
    {% endif %}

    {# Test lock table #}
    {{ 'stage4.connected.test.lock.title'|trans|colorize }}
    {% if test.lock is same as(true) %}
        {{ 'stage4.connected.test.success'|trans|colorize }}
    {% else %}
        {{ 'stage4.connected.test.fail'|trans|colorize }}
        <blockquote>{{ test.lock }}</blockquote>
        {{ 'stage4.connected.test.lock.error'|trans|colorize }}
    {% endif %}

    {# Test unlock table #}
    {{ 'stage4.connected.test.unlock.title'|trans|colorize }}
    {% if test.unlock is same as(true) %}
        {{ 'stage4.connected.test.success'|trans|colorize }}
    {% else %}
        {{ 'stage4.connected.test.fail'|trans|colorize }}
        <blockquote>{{ test.unlock }}</blockquote>
        {{ 'stage4.connected.test.unlock.error'|trans|colorize }}
    {% endif %}

    {# Test drop table #}
    {{ 'stage4.connected.test.drop.title'|trans|colorize }}
    {% if test.drop is same as(true) %}
        {{ 'stage4.connected.test.success'|trans|colorize }}
    {% else %}
        {{ 'stage4.connected.test.fail'|trans|colorize }}
        <blockquote>{{ test.drop }}</blockquote>
        {{ 'stage4.connected.test.drop.error'|trans|colorize }}
    {% endif %}

    {# Test cache table #}
    {{ 'stage4.connected.test.cache.title'|trans|colorize }}
    {% if test.cache is same as(true) %}
        {{ 'stage4.connected.test.success'|trans|colorize }}
    {% else %}
        {{ 'stage4.connected.test.fail'|trans|colorize }}
        {{ 'stage4.connected.test.cache.error'|trans|colorize }}
    {% endif %}

    {{ 'stage4.connected.test.results.title'|trans|colorize }}
    {% if not issues %}
        {{ 'stage4.connected.test.results.success'|trans|colorize }}
    {% else %}
        {{ 'stage4.connected.test.results.fail'|trans({'issues': issues})|colorize }}
    {% endif %}
{% else %}
    {{ 'stage4.connected.fail'|trans|colorize }}
    <div class="ui red message">{{ dberror }}</div>
{% endif %}
