{% translate_default_domain 'page_installer' %}

{{ 'stage9.subtitle'|trans|colorize }}

{# Pre-Install: before update core tables execute this code of install #}
{{ 'stage9.upgradeInstall.title'|trans|colorize }}
{{ 'stage9.upgradeInstall.paragraph'|trans|colorize }}
{{ 'stage9.upgradeInstall.log'|trans|colorize }}

<div class="ui segment">
    {% for message in upgradeInstall %}
        {{ message|colorize }}
    {% endfor %}
</div>

{{ 'stage9.building.title'|trans|colorize }}
{{ 'stage9.building.paragraph'|trans|colorize }}

{# Synchronization of core tables, you can found this entities in "src/core/Entity" #}
{{ 'stage9.sync.title'|trans|colorize }}
{{ 'stage9.sync.paragraph'|trans|colorize }}

<div class="ui segment">
    {% for message in synchronizationTables %}
        {{ message|colorize }}
    {% endfor %}
</div>

{# Insert data #}
{{ 'stage9.data.title'|trans|colorize }}
{{ 'stage9.data.paragraph'|trans|colorize }}

<div class="ui segment">
    {% for message in insertData %}
        {{ message|colorize }}
    {% endfor %}
</div>

{# Installation of modules #}
{{ 'stage9.modules.title'|trans|colorize }}
{{ 'stage9.modules.paragraph'|trans|colorize }}

<div class="ui segment">
    {% for message in installOfModules %}
        {{ message|colorize }}
    {% endfor %}
</div>

{{ 'stage9.ready'|trans|colorize }}
