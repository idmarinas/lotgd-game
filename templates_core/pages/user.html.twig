{% trans_default_domain textDomain %}

<form action="{{ 'user.php?op=search'|lotgd_url }}" autocomplete="off" method="POST" class="ui form">
    <div class="inline field">
        <label for="q">{{ 'default.form.search.label'|trans|colorize }}</label>
        <div class="ui action input">
            <input type="text" name="q" id="q">
            <button type="submit" class="ui button">{{ 'default.form.search.button.submit'|trans|uncolorize }}</button>
        </div>
    </div>
</form>

<br>

{% include '@core/pages/user/' ~ tpl|default('default') ~ '.html.twig' %}
